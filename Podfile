platform :ios, '17.0'

target 'iPadDx' do
  use_frameworks!

  # Real Capacitor bridge runtime — source from npm package
  pod 'Capacitor', :path => 'Bridges/capacitor_bridge/node_modules/@capacitor/ios'
  pod 'CapacitorCordova', :path => 'Bridges/capacitor_bridge/node_modules/@capacitor/ios'
end
